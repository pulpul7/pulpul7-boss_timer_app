# 작업 및 빌드 원칙

- 사용자 지시가 있기 전에는 실행 파일, 배포 ZIP, 테스트 빌드를 생성하지 않는다.
- 빌드 및 복사는 사용자가 수동으로 진행한다. 소스 수정과 필요한 비빌드 검증만 수행한다.
- 새 폴더에서 테스트할 필요가 있으면 먼저 필요성을 설명한다. 임의로 새 테스트 배포 폴더를 만들지 않는다.
- 기존 배포본, 사용자 설정, 서버별 스케줄, 음성 파일은 보존한다.

# 팝업 원칙

- 별도 지시가 없으면 메시지박스는 부모 윈도우 중앙에 자체 Tk 팝업으로 표시한다.
- Windows 기본 메시지박스(tkinter.messagebox)는 사용하지 않고 `_show_centered_messagebox`를 사용한다.
- 캡처 진행창은 측정 영역을 가리지 않는 게임창 상단에 배치한다.
