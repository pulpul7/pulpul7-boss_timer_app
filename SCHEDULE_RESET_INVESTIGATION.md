# 스케줄 교체 조사 (2026-09-11)

## 실제 파일에서 확인한 사실

- 조사 시작 시 `AppData/Roaming/BossTimer/server_profiles/season_17/9/schedule_state.json`에는 일반 이벤트 692건이 있었다. 빈 파일로 초기화된 상태는 아니었다.
- `schedule_last_import_meta.imported_at`: `2026-09-11T04:45:08`.
- 불러오기 출처: `github_data`, `data/schedules/오9.json`, 버전 `2026.09.07.008`.
- 불러온 자료의 시즌은 `1` / `오9 딸기 1차 시즌`, 저장 대상은 `season_17/9`였다.
- 조사 시작 시 작업 폴더의 시즌 17 main 캐시는 `2026.09.10.003`이었다.
- 작업 폴더 로그에는 04:45 불러오기 실행 기록이 없었다. 당시 실행한 다른 배포 폴더의 로그가 없으므로 **버튼 조작/자동 시작/다른 실행본 중 정확한 실행 계기는 아직 확정하지 않았다**.

## 재현 가능한 취약 경로와 수정

1. 새 실행 폴더의 최초 시즌 설정에서 공용 AppData 프로필을 읽은 후 다시 초기화할 수 있었다. 시작 시에는 저장 파일 또는 메모리 일정이 있으면 초기화하지 않는다. 명시적인 시즌 변경은 유지한다.
2. 저장된 빈 프로필에도 시작 캐시/원격 자료를 자동 적용할 수 있었다. 저장 파일이 없는 신규 프로필에만 자동 초기 자료를 허용한다.
3. 로컬 캐시의 시즌 일치 여부를 검사하지 않았다. 시즌이 명시된 다른 시즌 캐시는 자동 적용하지 않는다.
4. 동기화는 버전이 다르면 구버전도 적용했다. 구버전/다른 시즌/로컬 수정 자료의 교체는 기본값 '아니오' 확인창을 거친다. 다운로드 중 수정도 감지한다.
5. 동기화 중 대상 프로필이 바뀌면 적용을 취소한다.
6. 불러오기 직전 기존 프로필 파일을 같은 폴더의 `schedule_state.json.before_import_날짜_시각.json`에 보관한다. 백업 실패 시 적용하지 않는다. 기존 복구 이력의 날짜 정리와 독립적이다.
7. 상태 로드/저장/초기화/불러오기에 경로, 호출자, 건수, 실행 위치·PID 등을 기록한다. debug 로그가 활성화된 경우 기록된다.

현재 일정/설정/기존 백업을 직접 덮어써 복구하지 않았다. 실행 파일이나 배포 ZIP도 생성하지 않았다.

## TXT / 소수점 표시

- TXT는 기존 복사 범위 설정을 사용하되, 일반보스 `젠시간 보스이름`만 출력한다. 고정보스/휴식/점검/날짜 안내 줄은 제외한다.
- TXT 경로는 시간 정렬과 과거 생성이 꺼져 있어 순서 변경으로 자정이 반복 추론되지 않는다. 날짜 없는 시간을 기존 입력 규칙에 따라 다음 도래 시간으로 해석한다.
- `소수점 숨김`은 기본 체크이며 일반 입력/OCR1/OCR2/TXT 모두 Text 태그의 `elide`로 표시만 숨긴다. 입력 버퍼, 파싱 및 저장에 사용되는 소수점은 변경하지 않는다.
