# 초단위 정밀 캡처 — 1차 게임 검증용

기존 OCR1, OCR2, 스샷 찍기는 유지한다. 스케줄 파서는 소수점 시각 문법을 추가했다.
스샷 패널의 **초단위 찍기**는 현재 보이는 지역 한 장을 분석하고,
별도의 네이티브 Windows 캡처 worker가 시간 숫자 ROI만 추적한다.
현재 구현은 실제 게임 정확도 인증을 마친 기능이 아니라 계측·검증용이다.

## 사용

1. 오딘 클라이언트 크기를 **1600×900**으로 맞추고 보스 시간표를 연다.
2. 스케줄 입력 → 스샷 패널 → **초단위 찍기**.
3. 시간표/지역/창 위치를 유지한다. 다른 창으로 시간표를 가리지 않는다.
4. 진행창은 시간표 위쪽에 표시된다. 모든 대상 완료 시 조기 종료하며 최대 65초 추적한다.
5. 결과는 기존 OCR1 입력창에 표시된다. 다른 지역의 기존 OCR 결과는 유지한다.
   기존과 같이 사용자가 스케줄에 적용한다. 취소/창 변경 시 기존 입력을 덮어쓰지 않는다.

진행 중 관리 창은 잠시 숨기고 완료/취소 시 복원한다. 화면 크기 변경,
창 이동, ROI 가림, 시간표 제목/지역의 지속적인 픽셀 변경을 감지하면 중단한다.
제목/탭 guard는 단순 픽셀 검사이므로 게임 내부 모든 화면 전환을 보장하는 장치는 아니다.
실제 게임 애니메이션에서 오탐 여부를 확인해야 한다.

## 처리와 시간 규칙

- 최초 화면: BitBlt 바로 전/직후 `perf_counter()`와 중간값 T0 기록.
  화면 한 장에 대한 기존 OCR1 분석과 ROI 추적은 동시에 진행한다.
  OCR1의 기존 작은 영역 재시도는 그대로 사용하지만 OCR2로 자동 재시도하지 않는다.
- OCR 완료 전: 게임시계 + 위/아래 시간 후보 띠만 저장한다.
  ring buffer는 각 `16 × 초당 횟수`로 제한한다(약 16초; 기본값은 32개).
  초기 기록을 잃으면 추측하지 않고 정밀 확정을 거부한다. 전체 이미지 연속 저장은 없다.
- OCR 완료 후: 최초 OCR의 숫자 좌표로 후보 띠를 좁혀 분 또는 초 숫자만 추적한다.
  단위가 분리된 OCR 단어도 지원한다. 숫자 위치를 확인하지 못하면 해당 보스는 미확정이다.
- 1일 이상은 제외. 1시간 이상은 분 변경 1회, 1시간 미만은 초 변경 4회를 사용한다.
- 완료 후보마다 시간 문자열 작은 ROI를 한 번 OCR하여 예상 감소값인지 확인한다.
  게임시계도 4회 tick 후 한 번 확인한다. 매 프레임 OCR하지 않는다.
- countdown은 사용자 전제대로 **floor**로 계산한다.
  최초 표시 R에서 R−1로 내려가는 경계의 실제 남은 시간은 R이다.
  분 변경 경계도 최초 표시 분수(시간 포함)에 해당한다.
  `1시간 00분 → 59분 59초` 단위 전환은 별도로 검증한다.
- 이전 캡처 시작~새 캡처 종료가 변화 시각 구간이다. 중간값과 반폭을 보존한다.
  게임시계 tick으로 절대시각 위상을 맞추고 초 보스는 후보 중앙값을 사용한다.
  800ms 초과 캡처 간격 또는 650ms 초과 후보 불일치는 정밀 확정에서 제외한다.
- 게임시계와 countdown의 위상차는 게임시계 tick을 1초 주기로 연장한 기준에 대해
  `[-0.5, 0.5)`초로 기록한다. 별도의 임의 보정 상수는 적용하지 않는다.

## 소수점과 실제 정확도

계산 과정은 float/마이크로초 datetime을 유지한다. 입력 문법은
`16:43:02.55 파르바`, `164302.55 파르바`를 지원한다. `.55`는 550ms다.
입력은 소수점 1~6자리를 받으며, 출력은 최소 두 자리부터 원본 유효 자릿수를 유지한다.
스케줄 생성·주기 반복·컷·복원·메트릭 보정·편집용 텍스트·JSON 저장·디스코드
음성 예약 전송에서 원본 datetime의 소수점을 보존한다. 일반 목록/이미지의
짧은 시각 표시는 원본을 수정하지 않는다. 같은 정수 초라도 소수점이 다른 예약은
디스코드 예약 그룹과 중복 방지 키에서 서로 다른 목표 시각으로 유지한다.
원본 소수점 시각, 각 후보, tick 구간, 추정 오차, 지터는 메모리와
선택 서버 프로필의 `precision_capture_latest.json`에 보존한다.
파일은 해당 서버의 가장 최근 실행 결과 하나이며 이미지는 포함하지 않는다.

초읽기는 소수점이 포함된 스케줄 목표 datetime을 전달받는다. 별도 JSON을 재읽는
방식이 아니다. OS 타이머 호출에 필요한 밀리초 변환과 음성 재생 자체의 지연이 있으므로,
실제 오디오가 ±0.01초 정확도라고 보장하지 않는다. 기존 초 보정 UI는 바꾸지 않는다.
500ms 샘플링의 ±250ms는 단일 경계 추정치이며, 절대 젠 시간에는 게임시계 경계 오차도
합쳐진다. 소수점 저장 자릿수가 정확도를 보장하지 않는다. floor 가정, 실제 서버 젠과
게임 UI의 지연 관계는 게임에서 확인해야 한다.

## 디버그 / 실게임 확인표

기존 디버그 로그를 활성화하고 `precision` 항목과 JSON을 확인한다.
완료된 실행의 JSON에는 디버그 설정과 무관하게 최근 최대 4096줄의 계측 로그도 포함한다.
높은 빈도에서도 초기 OCR 정보가 사라지지 않도록 주요 로그는 `milestones`에 별도 보존한다.
`capture_stats`의 횟수/총 간격/최대 간격으로 실제 달성 빈도를 확인할 수 있다.

- `capture_t0`, `ocr1_start/done`, `tracker_start/end`: OCR 중 캡처가 계속되는지.
- `roi_capture`: 실제 BitBlt 시작/끝, ROI별 간격/지터. 최대 간격이 800ms 미만인지.
- `boss_ocr`, `roi`: 지역별 숫자 위치와 일/분/초 분류.
- `tick`, `candidate`, `verify`, `final`: 실제 감소 확인, 후보 시각과 오차.
- `clock_offsets_seconds`: 게임시계/countdown 상대 위상. 고정 편향이 있는지.
- `tracking_done`, `unconfirmed`: 완료 또는 제외 이유.

검증 순서: 4열/3열 지역, 초 보스 여러 개, 분 보스 여러 개, 두 유형 혼합,
OCR 중 첫 분 변경, `1시간 00분`, `00초 → 59초`, 자정, 1일 이상만 있는 지역,
65초 타임아웃, 취소, 창 이동/가림/지역 전환 순으로 확인한다.
표시 애니메이션이 픽셀 변경으로 잡히면 먼저 ROI를 좁히고 임계값을 조절한다.

스샷찍기 위 **⚙ → 촬영 설정**에서 연속촬영 횟수 2~10회를 선택한다.
후딜레이 설정 UI는 제거했으며 기존 스샷 후딜레이 내부 동작은 유지한다.
**감지영역 미리보기**를 체크하면 설정창에서 즉시 반투명 청록색 상자를 표시한다.
미리볼 챕터를 선택하면 해당 챕터의 실제 칸만 표시한다(5개=3+2, 7개=4+3).
미리보기 선택은 촬영 챕터를 강제하지 않는다. 실제 챕터는 OCR1이 판독한다.
최초 4초는 보스 수/분류와 무관하게 모든 고정 후보 시간 영역을 수집한다.
OCR이 먼저 끝나도 4초 전에는 대상을 제외하지 않으며, 이후 T0부터 수집한 기록을 재생한다.
설정창을 닫거나 촬영을 시작하면 상자는 사라지며 촬영 중에는 표시하지 않는다.
시간 영역은 좌우 각각 약 10%를 줄이고, 기존 위쪽 여백은 10px, 아래는 1px 줄였다.
4초 이후에는 실제 챕터에 존재하는 추적 대상의 숫자 ROI만 캡처한다.
진행 표시는 `총: N개 남은추적: N개 / 완료: N개 (배제 포함)`이며,
1일 이상/출현 중/미인식/검증 실패도 종료되면 완료에 포함한다. OCR 전에는 총수를 분석 중으로 표시한다.
스캔이 끝나면 스케줄 입력창을 복원하고 맨 앞으로 올린다.
오버레이는 클릭 통과 및 Windows 캡처 제외로 설정하며, 캡처 제외 설정에 실패하면
오탐 방지를 위해 상자를 표시하지 않고 `overlay_disabled` 로그를 남긴다.
환경설정의 초당 추적 횟수도 같은 2~10회 범위를 사용한다.
기본값은 초당 5회(분당 300회)다. 저장한 값은 다음 스캔부터 적용하며 재실행에도 유지된다.
4회는 250ms, 10회는 100ms의 목표 주기다. 실제 달성 주기는 CPU/캡처 지연에
따라 달라지므로 설정값만으로 정확도 향상을 보장하지 않는다.

## 자동 검증

`python -m unittest test_precision_capture test_schedule_fractional_time -v`

실시간 모의 화면에서 OCR을 1.3초 지연시켜도 초기 분 변경을 복원하는지,
복수 ROI 동시 추적/조기 종료/타임아웃/취소/소수점/자정/고정 ROI 범위를 검사한다.
Windows GDI 자체 읽기는 별도 소규모 smoke check로 확인했다.
2026-09-10 16:42 실행 로그: OCR 1.9045초, 추적 59.062초,
최대 간격 0.581623초, 최대 지터 0.081623초. 보스 7개와 게임시계 검증 성공,
티르는 1일 이상으로 제외됐다. 파르바 후보는 16:43:01.469493~01.521329,
최종 중앙값은 16:43:01.508488이었다. 이전 입력창의 16:43:02는 최종 반올림 결과다.
측정 불확실성은 약 ±0.51~0.56초이며 실제 서버 젠과 일치하는지는 별도 확인이 필요하다.
2026-09-10 17:28 기존 코드 2회/초 실측: 7개 대상 중 6개 정밀 확정,
페티는 `numeric_roi_or_units_unresolved`로 제외. 티르는 1일 이상 제외.
최대 캡처 간격 0.594680초, 최대 지터 0.094680초, 불확실성 약 ±0.52초.
원본: `build_precision_capture/live_measurements/20260910_172840_2hz.json`.
이는 실제 서버 젠과 비교한 절대 오차가 아니다. 분까지만 출력되는 항목은
정밀 미확정으로 최초 OCR 값을 유지한 것이며, 4초 수집만으로 모든 누락 해결을 보장하지 않는다.
최초 4초 수집 변경 당시에는 사용자 요청으로 추가 게임 실측을 하지 않았다.

## 22:51~22:54 사용자 테스트 누락 분석 및 후속 수정

`boss_timer_debug.log`에서 스칼라니르(22:51:28), 스바르트/두라스로르(22:53:03),
신마라(22:54:12)는 모두 `numeric_roi_or_units_unresolved`로 추적 전에 제외됐다.
이번 네 건은 시간은 OCR로 읽었지만 정밀 모듈이 분 숫자의 위치를 찾지 못한 경우다.
이 네 건의 로그에는 OCR 단어 좌표가 없어 세부 토큰 분리 형태까지는 확정할 수 없다.
다른 이전 실행(22:44)에는 0.8초 초과 캡처 간격으로 중단된 기록도 있다.

정밀 모듈에서 분리 숫자/단위와 단위 뒤에 붙은 텍스트를 조합하는 처리를 보완했다.
숫자 위치가 여전히 불명확하면 최초 T0 이미지의 작은 시간 영역만 한 번 재판독한다.
초기 남은 시간과 일치하고 숫자 ROI를 찾은 경우에만 채택한다. 매 프레임 OCR은 하지 않는다.
실패 시 `roi_unresolved`에 단어/좌표, `roi_retry`에 재판독 결과를 기록한다.
19개 모의 테스트 통과(작은 ROI 재판독 복구, 4초 초기 기록, 합계, 홀수 배치 포함).
이번 수정 후 실제 게임에서 네 보스의 누락이 해소됐는지는 사용자 재시험이 필요하다.

## 관측 좌표 상수와 불확실 입력 확인

`precision_capture_layout.py`에서 정밀 캡처 좌표를 관리한다.
보스 시간 영역은 이전 대비 왼쪽을 추가 20px 줄였다.
현재시간 영역은 `(190,256)~(420,306)`으로, 이전 대비 왼쪽 12px, 위/아래 각각 10px 줄였다.
미리보기, 초기 후보 수집, 보스별 시간 영역이 같은 상수를 참조한다.
`TIMER_COLUMNS`, `TIMER_ROWS`, `GAME_CLOCK_BOUNDS`, `BOSS_LEFT_TRIM`이 좌표 변경 지점이다.
`CHAPTER_BOSS_COUNTS`는 기본 칸 수이며 게임 목록 `RECORD_BOOK_BOSS_ORDER`가 주어지면
그 목록의 보스 수로 위 `(N+1)//2`, 아래 `N//2` 배치를 다시 구성한다.
일반 사용자 스케줄 보스 추가는 게임 UI 배치를 변경하지 않는다. 8칸 초과 배치는 명시적으로 거부한다.
기존 OCR1/OCR2의 인식 알고리즘과 좌표 생성은 변경하지 않는다.

기존 불확실 확인창은 이미 등록된 보스의 출현 중/1일 이상 입력만 검사했다.
이제 신규 보스도 포함해 출현 중, 1일 이상, 분/시간 단위 입력, OCR 경고/정밀 미확정 줄을 검사한다.
경고는 렌더링 당시 줄 내용과 정확히 일치할 때만 연결한다. 정상 정밀 확정 시간은 소수점이 있다는
이유만으로 불확실 처리하지 않으며, 수동 수정된 시간에 이전 보스 경고를 이름만으로 붙이지 않는다.

## 배포용 로그 기본값

`precision_debug_logging = false`가 기본이다. 샘플별 상세 로그는 메모리에만 유지하며,
`precision_capture_latest.json`을 새로 기록하지 않는다. 오류/완료 요약과 실제 측정 시간은 유지한다.
상세 파일 로그가 필요할 때만 로컬 INI에서 `true`로 설정한다. 수동 빌드/정리 사항은
`BUILD_HANDOFF.md`를 참고한다. 별도 지시 전에는 테스트 빌드를 만들지 않는다.
